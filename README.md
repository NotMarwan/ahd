# عهد · Ahd

**A bank that *witnesses, seals, settles, and nets* interest-free interpersonal loans (قرض حسن /
qard hasan) — and never lends its own money, never judges disputes, never charges interest or
penalty, and never scores credit.** Basis: Qur'an 2:282 (write the debt) + 2:280 (grace for the
struggling). Soul: «كلمتك محفوظة، وعلاقتك محميّة».

---

## The product → `project/ahd-app/`

One cohesive, offline, deterministic, **Arabic-RTL** app. Seven screens, one shell, one router, one
design language, over a parity-tested copy of the demo's golden engine.

```bash
node project/ahd-app/_serve-app.cjs   # → http://localhost:8124  (fully offline)
```

| # | Screen | What it is | Spine highlight |
|---|---|---|---|
| 🏠 | **الرئيسية** | front door: brand, live summary, the 2:282/2:280 basis | bank witnesses, never lends/judges/charges/scores |
| ➕ | **أنشئ عهدًا** | create a عهد with the **live riba linter** | blocks any penalty/interest clause, offers the halal fix, gates the seal |
| 📔 | **دفتري** | creditor home (لي/عليّ) + a bank-sent gentle reminder | overdue is **amber, never red**; no shaming counter; a finite merciful ladder |
| 📜 | **سِجلّ الشهادة** | the witness timeline — one feed of every sealed moment across your عهود | the bank *witnesses* → late=amber, dispute=neutral, no score |
| ♾️ | **القرض المفتوح** | open-term qard hasan + إبراء (forgiveness) | no due ⇒ **never overdue**; conservation exact |
| 👥 | **الدائرة** | treasurer dashboard: progress + dignified states + one sealed proof | the group reminder **never names the late**; «ذمّة المناسبة محفوظة» |
| 🔁 | **الدائرة+** | بالأصناف split · recurring auto-post · graduation قَيْد→عهد | mode-B pledge sketch carries a visible ⚠️ Shariah-review guard |
| 🔗 | **المقاصّة** | the tangle → fewest transfers (9→2) + consent legs + conservation | reuses the golden netting; «لا ريال يُخلق ولا يضيع، ولا فائدة» |

**Contextual screens** (reached in-flow, no nav pill, to keep the nav clean): **حافظة الإثبات** (proof-pack
— the sealed doc + a live tamper-verify, from دفتري) · **محلّ خلاف** (dispute-pause — «يشهد ولا يحكم»,
from a disputed row) · **الإعدادات · عن عهد** (Arabic-Indic digit toggle + the «ما لا نفعله» manifesto,
from the home footer).

**Architecture:** pure DI feature modules (`features/*.js`, Node-testable) + a screen registry/router
(`app.js`) + innerHTML screens (`screens/*.js`) + `engine.js` (a **byte-faithful, parity-proven**
copy of the demo's golden logic, generated by `build-engine.cjs`). See `docs/ARCHITECTURE.md`.

## The test gate (keep green)

```bash
cd 10_Deep/Hardening/test-harness
node run-tests.cjs && node offline-check.cjs && node dom-smoke.cjs   # demo core: 184/0
node app/run-app-tests.cjs                                           # app: 17 suites, 575/0
```
**759 assertions total**, all green. Determinism is enforced (no `Date.now`/`Math.random`/`Intl`/
float money in logic; integer halalas, 1 SAR = 100). The golden crypto/netting functions are
**called, never modified**; `app/engine-parity.cjs` + `app/golden-vectors.test.cjs` guard the copy.

## Legacy / frozen reference → `project/ahd-demo/`

The original single-file presenter demo, **kept byte-for-byte frozen** (tripwire SHA-256
`e2f48467…`) as a known-good offline fallback that runs the golden hash-chain + netting live. It is
**superseded by `ahd-app/`** and is never edited. See its README and decision **D-4**.

## The spine (non-negotiable)

- Bank witnesses/seals/settles/nets; does **not** lend, judge, charge on the loan, or score.
- **No riba / penalty / maysir / gharar.** Integer halalas, never float money.
- Trust signal is a **qualitative own-history word** («وفّى بعهوده») — never a number, never exported.
- **AI issues no fatwa** — cite scholars/standards/verses; flag Shariah questions, don't rule.
- "Late" is **amber, never red**. Dignity over everything.

## Where things are

- `CLAUDE.md` — the working guide (spine, rules, layout).
- `OVERNIGHT-LOG.md` — running work log + morning briefing.
- `DECISIONS-FOR-MARWAN.md` — open decisions (Shariah-gated / irreversible).
- `docs/` — `ARCHITECTURE.md`, `PUBLISHABLE-PRODUCT-SPEC.md`, `PRESENTER-GUIDE.md`,
  `DECK-DRAFT-AR.md`, `evidence/`, `superpowers/plans/`.
- `project/ahd-promo/` — the Remotion motion promos. **`out/ahd-new-features.mp4`** films the 4 new
  features (60fps, Arabic captions); `out/ahd-app-promo.mp4` films the original surface. Render the
  new one: `cd project/ahd-promo && npm run render:new`.
