# HomeCard — بطاقة مدخل

Primary entry card (grid: icon + title + subtitle), used as a `<button>`.

- Stack: `.hcards` column → `.hcard` per entry → `.hico` (emoji badge on teal-soft), `.ht` (bold title), `.hs` (muted subtitle, auto second row).
- Real product copy is Arabic-first; keep titles short (2-3 words).
