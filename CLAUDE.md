# CLAUDE.md — عهد (Ahd) project guide

**What this is.** Ahd is a Saudi Islamic-finance prototype: a bank that **witnesses, seals, settles, and nets** interest-free interpersonal loans (قرض حسن / qard hasan) — but **never lends its own money, never judges disputes, never charges interest or penalty, and never scores credit.** Basis: Qur'an 2:282 (write the debt) + 2:280 (grace for the struggling). Soul: «كلمتك محفوظة، وعلاقتك محميّة».

## The spine (non-negotiable — never violate)
- Bank witnesses/seals/settles; does **not** lend, judge, charge on the loan, or score.
- **No riba / penalty / maysir / gharar.** Integer halalas (1 SAR = 100), never float money.
- Trust signal is a **qualitative own-history band** («وفّى بعهوده»), never a number, never exported, never underwrites.
- **AI issues no fatwa** — cite scholars/standards/verses; flag Shariah questions, don't rule.

## Two builds (both on the same git tree)
- **`project/ahd-demo/index.html`** — the FROZEN presenter demo. **NEVER edit it.** It is pinned by a tripwire SHA-256 `e2f48467a70a958be0840dd9f0f9fca27c47bb35445481f19ba27de0d1b8be40` (`sha256sum -c _overnight/backup/demo.sha256`). Its pure logic lives between the `// ===AHD-LOGIC:BEGIN/END===` markers. **Golden-pinned functions — call, never modify:** `sha256`, `canonical`, `sealBlock`, `recomputeSeal`, `verifyRecord`, the netting core + tiebreak, `fmt`, `respread`. Golden main seal `6c9410b9…`.
- **`project/ahd-app/`** — the publishable app (6 screens: home, create, دفتري, القرض المفتوح, الدائرة+, المقاصّة). It reuses a **byte-faithful copy** of the demo engine: `engine.js` is GENERATED by `build-engine.cjs` from the demo slice; `app/engine-parity.cjs` proves the copy. Run it: `node project/ahd-app/_serve-app.cjs` → `http://localhost:8124` (fully offline). Architecture: pure DI feature modules (`features/*.js`, Node-testable) + a screen registry/router (`app.js`) + innerHTML screens (`screens/*.js`).

## Hard rules for any change here
1. **Never touch the demo or the golden functions' internals.** Build additively in **new files**.
2. **Determinism:** no `Date.now`/`new Date`/`Math.random`/`Intl`/`.toLocaleString`/float money in logic. Fixed `AS_OF`. (Enforced by `app/app-offline.test.cjs`.)
3. **TDD:** write the failing test first. Tests live in `10_Deep/Hardening/test-harness/app/` (auto-discovered by `run-app-tests.cjs` if named `*.test.cjs`/`*-parity.cjs`/`*-smoke.cjs`).
4. **The harness is the gate — keep it green, never weaken an assertion:**
   ```
   cd 10_Deep/Hardening/test-harness
   node run-tests.cjs && node offline-check.cjs && node dom-smoke.cjs   # demo core: 184/0
   node app/run-app-tests.cjs                                           # app: 11 suites
   ```
5. **Anything touching the spine, a Shariah ruling, the golden vectors, or anything irreversible → `DECISIONS-FOR-MARWAN.md`, don't decide it alone.**

## Where things are
- `OVERNIGHT-LOG.md` — running work log + the morning briefing (deliverables index, how-to-review).
- `DECISIONS-FOR-MARWAN.md` — open decisions (D-1 self-disclosure, D-3 mode-B pooled deposit — Shariah-gated).
- `docs/` — `ARCHITECTURE.md`, `PUBLISHABLE-PRODUCT-SPEC.md`, `PRESENTER-GUIDE.md`, `DECK-DRAFT-AR.md`, `evidence/` (graded brief + rebuttal playbook + demand-survey kit).
- `docs/superpowers/plans/` — one TDD plan per feature.
- `10_Deep/Ledger/` — decisions register, sources ledger, open-threads (OT-IDs).
- `12_Consumer/Agent-{1..4}/` — the consumer feature specs. `13_Circle/` — the Circle.
