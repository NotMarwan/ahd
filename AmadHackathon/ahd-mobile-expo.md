# تطبيق الجوال — ahd-mobile (Expo)

> source: `application/ahd-mobile/`، الخطة: `docs/superpowers/plans/2026-07-15-mobile-full-screens.md`، الفرع: `feat/mobile-screens`

## الحالة (2026-07-15 مساءً)
- **23 شاشة كاملة** (5 تبويبات + 18 شاشة سياقية عبر «المزيد») — تعادل تطبيق الويب.
- المقاصّة أُصلحت: كانت ترسل أسماء غريبة عن المحرك (سالم/ماجد → تُسقط، «قبل 1 بعد 0»). الآن شبكة `engine.IOUS`: 9 التزامات بين 5 أعضاء → تحويلان، «المجموع محفوظ».
- أيقونات SVG للتبويبات، شريط 56px، الرئيسية أقصى اليسار، الشعار منزّل.
- جولة تجريبية موجّهة (زر بالرئيسية → شريط «التالي» يمشي: إنشاء → ختم → دفتري → تفاصيل → مقاصّة → إثبات → المزيد) + زر «تعبئة تجريبية».
- البوابة: `npm test` داخل `application/ahd-mobile` = **28 suites / 56 tests أخضر**، `tsc --noEmit` نظيف، `expo lint` صفر أخطاء، tripwire سليم.
- حارس تكافؤ: كل route في السجلّ له ملف فعلي (اختبار يفشل عند النقص).

## التشغيل
- جوال حقيقي: حدّث Expo Go من المتجر (SDK 57)، افتح `exp://<IP-الجهاز>:8081` بعد `npx expo start` داخل `application/ahd-mobile`.
- المحاكي على هذا الجهاز يحتاج تفعيل virtualization من BIOS (مقفل حاليًا).

## ملاحظات مفتوحة
- الفرع `feat/mobile-screens` غير مدموج في main بعد — قرار الدمج للمالك.
- شاشة «سِجلّ المعروف» بنيت على `engine.TRUST` (شرائح نوعية فقط) لأن `daily.js` الويب وحدة مختلفة — يُراجَع أنها تطابق النية.
