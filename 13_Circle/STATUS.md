# 13_Circle · STATUS

**DONE** — «الدائرة» unified (سعود Standing + لُجين Occasion) into one `Circle` object with a `mode`; screens G1–G4 built on `project/ahd-demo/index.html`, reusing the existing engine (respread / fold / sha256 seal / Nafath confirm / grace 2:280 / إبراء / DISPUTED / netting + consent). Seeded «رحلة العلا» (occasion) + «شقة الملقا» (standing); the 4 ربع circles' open shares reproduce the demo's frozen 9-IOU tangle and net 9→2 through the **existing** Muqassa. Dignity preserved (organizer-only status, group reminders, grace, generous إبراء, no number/score). No golden-pinned function modified; no passing assertion deleted.

**Harness:** 135 (run-tests) + 9 (offline) + 40 (dom-smoke) = **184 passed, 0 failed** (was 145; +39 new, exit 0/0/0).

**Presenter path:** R[0] «ابدأ من الدائرة» → G1 create → G2 receive → G3 dashboard (→ close «ذمّة المناسبة محفوظة») → G4 → `go(5)` existing Muqassa (9→2 + conservation proof).
