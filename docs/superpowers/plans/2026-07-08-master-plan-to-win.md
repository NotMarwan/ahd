<div dir="rtl">

# 🏆 الخطّة الرئيسة للفوز الحاسم — 8 → 18 يوليو 2026

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. هذه خطّة اليوم-باليوم الحاكمة؛
> كلّ الخطط السابقة (judge-lens · front1 · front3 · road-to-9) تصبّ فيها. Checkboxes تُعلَّم عند الإنجاز.
>
> **تقسيم المسؤوليّة (قرار المشغِّل 2026-07-08):** المشغِّل عليه **الاستبيان فقط** (+ ما لا يُنسَخ:
> جسده في البروفة، وقراره في D-JL-1، وتسليم قالب المنظّمين والأسماء إن وُجدت). **كلّ ما عداه على Claude-E.**
>
> **قواعد الصرامة (كلّ يوم، بلا استثناء):**
> 1. البوّابة خضراء قبل أيّ push: `cd tests && node run-all.cjs` → `AHD GATE ✅` + tripwire.
> 2. أيّ سطح يراه المحكّم تحت 8/10 = لا يُعتمد — يرجع يتصلّح (CLAUDE.md قاعدة 6).
> 3. العمود الفقريّ فوق الفوز — أيّ تعارض → `DECISIONS-FOR-MARWAN.md`.
> 4. كلّ جلسة تنتهي بـ: DONE line في STATUS + push. الريموت دائمًا أحدث حالة خضراء.
> 5. قصّ من الأسفل عند الضيق (JL-4 ثم الإضافيّات) — **أبدًا من العرض ولا من البروفة.**

---

## 📅 8 يوليو (اليوم) — إغلاق الذيول المفتوحة

- [ ] **T8.1 · توثيق أسماء فائزي 2025 كتابيًّا** — وكيل ويب (sonnet) يمشّط منشورات طويق/الإنماء/فينتك السعودية
  المكتوبة (لينكدإن/X/إنستقرام web) حول 2–3 أغسطس 2025 لتثبيت تهجئة «كوانتم…/حصالتي/سنتري» وأسماء الأعضاء.
  النتيجة → تحديث `docs/research/amad-winners-research-ar.md` (🟡→🟢). فشل البحث = يبقى 🟡 ولا يُذكر اسم شخصٍ علنًا.
- [ ] **T8.2 · إعادة تصوير الشاشات بعد التلميع** — كلّ لقطات `reshoot-after-JL-2`: المسار الذهبيّ السبعة +
  «أثر عهد» + المجموعة القديمة في `docs/pitch/fallback/`. الأداة: preview server + screenshot؛ إن استمرّ عطل
  الـpipeline → `npx playwright screenshot` (محلّيّ، إن وُجد) أو التقاط يدويّ موجَّه في جلسة قادمة — **لا يُؤجَّل بعد 10 يوليو.**
- [ ] **T8.3 · فحص المسار الذهبيّ نقرةً نقرة** (شبه بروفة آليّة): عبر preview أتحقّق أنّ كلّ خليّة «افعل» في
  `script-ar.md` تهبط على زرّها بالنصّ المكتوب حرفيًّا؛ أيّ فرق → تصحيح السكربت/الدليل فورًا.

## 📅 9 يوليو — تطوير المنتج أوّلًا (قرار المشغِّل 8 يوليو مساءً: «نكمل التطوير قبل العرض»)

- [ ] **T9.0 · JL-4: شاشة «الضمانات والحدود»** — تُغلق OT-P1other + P15 معًا؛ الخطّة:
  `2026-07-08-front4-bounds-panel.md` (TDD كاملة، ضماناتٌ كلّ واحدةٍ باختبارها). بعدها لقطة شاشةٍ جديدة
  تنضمّ للمجموعة + سطرٌ في السكربت لمقعد الأسئلة.
- *(بناء العرض pptx ينزاح إلى 10–11 — يبقى قبل Gate B بيومين كاملين.)*

## 📅 10–11 يوليو — بناء العرض (pptx) v1 (كان يوم 9)

- [ ] **T9.1 · بناء `docs/pitch/ahd-amad-deck.pptx` v1** من `deck-content-v2.md` عبر skill الـpptx:
  - **إن وصل قالب المنظّمين** → التعبئة في القالب (خطّة front1 T2 كما هي: unpack، تعبئة، grep صفر placeholders).
  - **إن لم يصل** → **بناء مستقلّ أنيق بنفس بنية الـ14 خانة** (نفس المقاس 10″×5.62″، هويّة عهد: العاجيّ/الأخضر
    الداكن/الذهبيّ من `app.css`) — جاهز للنقل الحرفيّ للقالب لاحقًا في ساعة. **لا نبقى محجوزين على ملفّ.**
  - `{TEAM}`: إن لم تصل الأسماء → شريحة الفريق باسم الفريق فقط + خانات فارغة مسمّاة، تُملأ عند التسليم.
- [ ] **T9.2 · لقطات T8.2 تُغرس في الشرائح** + تحديث `deck-build-notes.md` (خريطة الأشكال للتعديل السريع).

## 📅 10 يوليو — ⛔ بوّابة صلبة + التقوية

- [ ] **G10 · بوّابة القالب:** آخر موعد لقالب المنظّمين والأسماء. بعده نعتمد النسخة المستقلّة رسميًّا ونحسّنها
  بدل الانتظار. *(تذكير للمشغِّل يوم 9 مساءً.)*
- [ ] **T10.1 · محاولة OT-CITE:** وكيل ويب يبحث عن مصدر أوّليّ محدَّث (وزارة العدل/ناجز 2024–25) لأرقام
  التنفيذ. وُجد → تحديث الأدلّة بعمرٍ جديد. لم يوجد → تبقى لافتة «2020–21» (صادقة كما هي).
- [ ] **T10.2 · تقييم JL-4** (عمق يراه المحكّم): أُقيّم «لوحة حماية المستدين» و«صقل حافظة الإثبات» ضدّ سؤال
  واحد: هل تُقوّي الدقائق الثلاث؟ نعم لواحدة فقط → خطّة مصغّرة TDD وتنفيذ 11–12. لا → تُقفل JL-4 رسميًّا بلا بناء.

## 📅 11–12 يوليو — التنفيذ الاختياريّ + جولة التنظيف

- [ ] **T11.1 ·** تنفيذ JL-4 المصغّرة (إن قرّرنا نعم) — بنفس صرامة front3 (اختبار يفشل أوّلًا، بوّابة خضراء).
- [ ] **T11.2 · تحديث الفيديو الترويجيّ** (promo/ بالـUI الجديد) — **اختياريّ، يُقصّ أوّلًا**؛ فقط إن كان كلّ ما فوقه أخضر.
- [ ] **T12.1 · جولة تنظيف شاملة:** grep أرقام قديمة في كلّ ما يراه المحكّم · مراجعة أخيرة لـ top6-cards ضدّ
  «درس سنتري» (الرفض هويّة) · تحقّق أنّ الديدن (README) متّسق مع كلّ الأوراق.

## 📅 13 يوليو — دمج الاستبيان + عدّة البروفة

- [ ] **T13.1 · دمج نتائج الاستبيان** *(مدخل المشغِّل — لو وصلت ≥30 استجابة)*: التحليل (مجاميع فقط) →
  شريحة البيانات (خانة 5) + بطاقة Q-A1 تنقلب من 🔴 إلى 🟢 «سألنا فعلًا». **ما وصل → الأوراق تبقى صادقة كما هي، بلا اختراع.**
- [ ] **T13.2 · عدّة البروفة النهائيّة:** صفحة واحدة للطباعة (`docs/pitch/stage-pack-ar.md`): تجهيز ما قبل
  الصعود (3 نوافذ) + جدول البيتات + البطاقات الستّ + دليل الأعطال — كلّ ما يحتاجه الواقف على المسرح في ورقة.

## 📅 14 يوليو — 🧑‍⚖️ Gate B: لجنة التحكيم النهائيّة

- [ ] **T14.1 ·** اللجنة السداسيّة كاملة (5 معايير + المحكّم المتعب) على: الـpptx النهائيّ + السكربت + اللقطات
  الجديدة + شاشة أثر عهد + بيانات الاستبيان إن وُجدت. **العتبة: ≥9 تقنيّ/UX/لا-يُنسى، ≥8.5 الباقي.**
- [ ] **T14.2 ·** جولة إصلاح فوريّة لكلّ ما دون العتبة + إعادة تقييم ما تغيّر.

## 📅 15 يوليو — 🧊 التجميد

- [ ] **T15.1 · FREEZE:** آخر sweep أرقام (البوّابة تُشغَّل، الأرقام تُثبَّت في كلّ الأوراق) → commit →
  **tag `v0.2.0-freeze`** → push. بعده: لا ميزة جديدة، إصلاح كسر فقط.
- [ ] **T15.2 ·** تحديث الذاكرة + STATUS بحالة التجميد وتعليمات صباح 18 (إعادة البوّابة وتثبيت الرقم النهائيّ).
- *(المشغِّل: جلسة صناعة العرض الإثرائيّة اليوم + بروفة كاملة بالعدّة.)*

## 📅 16–18 يوليو — الميدان (حضوريّ)

- [ ] **T16 ·** جلسة دعم عن بُعد عند الطلب: أيّ قصّ/تكييف يفرضه المنظّمون على السكربت أو الشرائح — بنفس قواعد
  الصرامة (بوّابة خضراء، لا كسر عمود فقريّ).
- [ ] **T18-صباحًا ·** إعادة البوّابة على جهاز العرض (`node run-all.cjs`) وتثبيت الرقم في الطرفيّة المفتوحة +
  الشرائح إن تغيّر. ثم: المسرح لك.

---

## 👤 قائمة المشغِّل الكاملة (كلّ ما لا أستطيع نسخه)

| # | البند | الموعد الحاسم |
|---|---|---|
| 1 | **الاستبيان** (`docs/evidence/DEMAND-SURVEY-KIT.md`) — التوزيع وجمع ≥30 | النتائج قبل **13 يوليو** |
| 2 | قالب المنظّمين + أسماء/صور الفريق (إن متاحة) | قبل **10 يوليو** وإلّا اعتمدنا المستقلّ |
| 3 | بروفة مصوّرة ×3 بالسكربت + بروفة كاملة يوم 15 | 13–15 يوليو |
| 4 | قرار D-JL-1: المستودع عامّ قبل التحكيم؟ | قبل **16 يوليو** |

## مقياس النجاح النهائيّ
يوم 15 مساءً: **pptx نهائيّ ولقطات جديدة ولجنة Gate B ≥ أهدافها والبوّابة `AHD GATE ✅` و`v0.2.0-freeze` مدفوعة** —
ويوم 18: مقدِّمٌ تمرّن ثلاثًا، بيده ورقة واحدة، وخلفه منتجٌ لا يستطيع الكذب.

</div>
