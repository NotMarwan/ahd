# Data Rigor + Judge Lens Merge

## Goal

Ship a portable frozen-demo tripwire and a source-bounded Data Rigor evidence ladder, then verify and fast-forward `judge-lens-real-leap` to `main`.

## Constraints

- Never edit `demo/index.html`, golden functions, or `app/engine.js`.
- TDD, integer-only logic, offline app, no trust score or individual profiling.
- GASTAT is context, not a personal-loan distribution; Findex is primary, Nafith is secondary, market sizing is a model.
- Preserve and exclude unrelated dirty files; stage only the documented whitelist.

## Work

1. Replace the Windows-incompatible `sha256sum` shell call with a Node `crypto` tripwire plus teeth.
2. Add immutable Findex, emergency-funds, Nafith, and GASTAT context data; complete integer market band.
3. Render a visible evidence ladder on the impact screen; classify sources by measured/illustrative and primary/secondary/model.
4. Update evidence brief, pitch source, script, screenshot, project map, metadata, and Obsidian cockpit.
5. Run targeted, full, and clean-snapshot gates; perform Judge Lens review; commit whitelisted changes; push branch then fast-forward `main`.
